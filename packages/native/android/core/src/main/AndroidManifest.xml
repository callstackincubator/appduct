<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application>
        <!--
          Captures the application Context before any app code runs, so `Cordierite.register(...)`
          works from `Application.onCreate()` with no manual init call
          (docs/tasks/19-android-entry-points.md). `${applicationId}` is filled in automatically by
          AGP: every consumer app gets a uniquely named authority, as the platform requires for a
          ContentProvider. The name below is fully qualified, not the relative `.CordieriteInitProvider`
          shorthand, because this module's Kotlin package (`com.callstackincubator.cordierite`)
          differs from its AGP `namespace` (`com.callstackincubator.cordierite.core`, see
          build.gradle) that relative names resolve against; a relative name here would resolve to
          a class that doesn't exist and crash at provider-install time.

          Opt out (an app that wants to drive initialization itself instead) with, in the app's own
          manifest:
            <provider android:name="com.callstackincubator.cordierite.CordieriteInitProvider"
                       tools:node="remove" />
          See packages/native/android/README.md, "Opting out of the init provider and trampoline".
        -->
        <provider
            android:name="com.callstackincubator.cordierite.CordieriteInitProvider"
            android:authorities="${applicationId}.cordierite-init"
            android:exported="false" />

        <!--
          No-UI trampoline for `${cordieriteScheme}://` deep links. The consumer app sets
          `manifestPlaceholders["cordieriteScheme"] = "myapp"` in its own build.gradle; forgetting
          it is a manifest-merger error at build time (see packages/native/android/README.md's
          troubleshooting section for the exact message). Calls `Cordierite.handle(intent)` and
          finishes immediately; a link with no Cordierite payload is not forwarded anywhere else
          (see CordieriteLinkActivity's own doc comment and the README for what to do if you need
          the same scheme for your own deep links too).

          Opt out the same way:
            <activity android:name="com.callstackincubator.cordierite.CordieriteLinkActivity"
                      tools:node="remove" />
        -->
        <activity
            android:name="com.callstackincubator.cordierite.CordieriteLinkActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${cordieriteScheme}" />
            </intent-filter>
        </activity>
    </application>
</manifest>
